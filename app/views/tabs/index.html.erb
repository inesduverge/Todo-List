<%= link_to "Create a new tab", new_tab_path %>
<ul>
  <% @tabs.each do |tab| %>
    <li><%= link_to tab.titulo, tab_path(tab.id) %></li>
  <% end %>
</ul>

<%= form_for @tab do |f| %>
  <%= f.label :titulo %>
  <%= f.text_field :titulo %>
  <%= f.submit "Create tab" %>
<% end %>
  
